package com.callor.hello.arrays;

public class ArraysAB {
	public static void main(String[] args) {

		int num = 0;
		for (int i = 0; i < 5; i++) {
			int index = i + 1;
			int numindex = 0;
		
		
		// for (int i = 0; i < 5; i++) {
		//	int num = i + 1; 
		//			(int) (Math.random() * 50) + 51;
			
			/*
			 * num 라는 변수와 index라는 변수가 서로 합성되어 하나의 변수가 새로 만들어짐
			 */
		}
	}
}
